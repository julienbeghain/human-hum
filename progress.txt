## Iteration 1: packages/db setup (human-hum-1fo)

- Created packages/db with Drizzle ORM + Neon serverless driver
- Star schema: artists, albums, tracks (dimension tables) + scrobbles (fact table)
- Unique constraints: artists(name, mbid), albums(name, artist_id, mbid), tracks(name, artist_id, mbid), scrobbles(track_id, listened_at)
- Source enum: lastfm, spotify, tidal
- Exports: db client, schema, Database type, and createDb factory
- Added DATABASE_URL to turbo.json globalEnv to suppress lint warnings
- Used generatedAlwaysAsIdentity() for auto-incrementing PKs (Postgres identity columns)
- Note: migration not run yet — requires DATABASE_URL env var pointing to Neon

## Iteration 2: LastFM single-page import script (human-hum-4uh)

- Created apps/import with tsx runner for LastFM import
- Fetches one page (50 tracks) from user.getRecentTracks API
- Upserts artists, albums, tracks with select-then-insert-then-refetch pattern for race safety
- Inserts scrobbles with onConflictDoNothing for dedup (unique on track_id + listened_at)
- Skips "now playing" entries (no timestamp)
- Uses `drizzle-orm` eq/and for type-safe WHERE conditions
- Env vars: LASTFM_API_KEY, LASTFM_USER, DATABASE_URL (added to turbo.json globalEnv)
- Run with: `pnpm --filter import run import`

## Iteration 3: /scrobbles page (human-hum-bi7)

- Created apps/web/app/scrobbles/page.tsx as a Server Component (no 'use client')
- Query uses Drizzle select().from().innerJoin() chain — no relations defined, so explicit joins needed
- Joined scrobbles → tracks → artists to get track name and artist name
- Added @workspace/db and drizzle-orm as dependencies to apps/web
- Added @workspace/db to transpilePackages in next.config.mjs
- Used Intl.DateTimeFormat for locale-aware timestamp formatting
- shadcn Table component is 'use client' but works fine when rendered from a Server Component
- PAGE_SIZE=50, ordered by listenedAt desc
- Empty state message when no scrobbles exist

## Iteration 4: Remove mbid from unique indexes (human-hum-180)

- Dropped mbid from unique indexes: artists(name), albums(name, artist_id), tracks(name, artist_id)
- mbid kept as nullable non-unique column — still stored but not part of entity identity
- Ingestion entity resolution simplified: match by name (+ artist_id) only, no conditional mbid conditions
- Test DDL must be kept in sync with schema changes — both ingestion.test.ts and queries.test.ts have inline SQL
- `drizzle-kit generate` auto-creates DROP INDEX + CREATE INDEX migration (no manual SQL needed)
- onConflictDoNothing() in resolveEntity still works — it just targets the new unique indexes now
- PR #28, migration: 0001_bizarre_shooting_star.sql

## Iteration 5: Move album_id from tracks to scrobbles (human-hum-3c4)

- Moved album_id FK from tracks to scrobbles — album is a property of a listen event, not the track entity
- Migration 0002_rare_thunderball.sql: adds column to scrobbles, copies data, then drops from tracks
- Auto-generated drizzle-kit migration needed manual edit to add UPDATE step for data migration
- recordListen() updated: albumId now goes in scrobble insert instead of track resolve values
- Test DDL in both test files must stay in sync with schema (inline SQL, not auto-generated)
- Dependent tasks (human-hum-0nb, human-hum-0dr) now unblocked for ingestion/query updates

## Iteration 6: Update queries and /scrobbles page for new schema (human-hum-0dr)

- Added leftJoin to albums table in getRecentScrobbles() — album_id is nullable so leftJoin not innerJoin
- albumName added to ScrobbleRow type as `string | null`
- /scrobbles page now shows Album column with em-dash (—) fallback for null
- Test added for album presence; existing test asserts albumName is null when no album provided
- No test script in packages/db/package.json — run via `cd packages/db && pnpm exec vitest run`

## Iteration 7: Extract import core into packages/db/importers (human-hum-i7v)

- Created packages/db/src/importers/lastfm.ts with importScrobbles(db, options) function
- Options: apiKey, user, from?, to?, onProgress? — designed for pagination follow-up (human-hum-99q)
- Added `from` and `to` params mapped to LastFM API's unix timestamp filters
- onProgress callback receives {page, totalPages, imported, skipped} per page
- Returns ImportResult: {totalImported, totalSkipped, pagesProcessed}
- CLI wrapper (apps/import/src/lastfm.ts) reduced to ~30 lines: env vars + call importScrobbles
- Removed drizzle-orm direct dependency from apps/import (only @workspace/db needed now)
- Added export "./importers/lastfm" to packages/db/package.json exports map
- `satisfies Source` used to type-check literal strings against the source enum without casting

## Iteration 8: Paginated backfill with rate limiting (human-hum-99q)

- Added `backfill` option to ImportOptions — when true, paginates all pages at 200 tracks/page
- Without backfill, single page of 50 (preserves existing behavior)
- Extracted `buildUrl()` and `fetchWithRetry()` helpers to keep the main loop clean
- Exponential backoff on HTTP 429: BASE_DELAY_MS * 2^attempt, up to MAX_RETRIES=5
- 200ms inter-page delay to respect LastFM rate limits
- CLI: `--backfill` flag parsed from process.argv, no arg parser dependency needed
- Added `import:backfill` script alias in apps/import/package.json
- do/while loop with currentPage/totalPages — totalPages comes from first API response's @attr

## Iteration 9: Incremental sync mode (human-hum-wy2)

- Default CLI mode (no flags) = incremental sync: queries MAX(listened_at) - 1 second as `from`
- 1-second overlap window handled by existing unique constraint dedup (onConflictDoNothing)
- Falls back to full backfill recursively when DB is empty (no scrobbles yet)
- Both incremental and backfill modes now paginate at 200/page with inter-page delay
- Added `getLatestScrobbleTimestamp()` query using `max()` aggregate from drizzle-orm
- Recursive call to `importScrobbles` with `backfill: true` keeps the fallback logic in one place
- CLI wrapper simplified: no flag = sync, `--backfill` = full history re-import
- `backfill: backfill || undefined` pattern avoids passing `false` which would skip sync detection

## Iteration 10: Backfill resume + completeness check (human-hum-3lm)

- Resume interrupted backfill: auto-set `to = MIN(listened_at) + 1s` so only older pages are fetched
- `getEarliestScrobbleTimestamp()` and `getScrobbleCount()` added to queries.ts using `min()` and `count()` aggregates
- Completeness check: after backfill, calls `user.getInfo` API to get remote playcount, compares with local COUNT(*)
- `CompletenessResult` type: { localCount, remotePlaycount, coveragePercent }
- Coverage will never be 100% — LastFM counts "now playing" entries and deleted tracks in playcount
- Backfill resume only activates when `to` is not explicitly set — explicit `to` takes precedence
- `let { from, to } = options` — destructuring with `let` allows reassignment in the function body

## Iteration 11: Offset pagination for getScrobbles (human-hum-asg)

- Changed getScrobbles return type from `ScrobbleRow[]` to `{ rows: ScrobbleRow[], totalCount: number }`
- Replaced `limit` param with `page` (default 1) and `pageSize` (default 50)
- Parallel COUNT(*) query via Promise.all for totalCount — same joins and WHERE clause as main query
- Extracted `baseQuery()` and `countQuery()` helpers to DRY up the shared join chain
- Cursor-based pagination still works alongside offset pagination (cursor adds a WHERE condition)
- Breaking change rippled to: /scrobbles page (destructure `{ rows }`), lastfm importer (destructure + rename `limit` → `pageSize`)
- `as ScrobbleRow[]` cast needed because Drizzle's select with joins infers a wider union type
- 4 new tests: totalCount alongside rows, page/pageSize pagination, empty page beyond data, totalCount respects filters

## Iteration 12: App shell with collapsible sidebar (human-hum-ti9)

- Created AppSidebar component with sidebar-07 pattern: collapsible="icon", SidebarRail for resize
- Flat nav items (Scrobbles, Artists, Albums, Stats) with Tabler icons, active state from usePathname()
- Header branding uses SidebarMenuButton size="lg" with render={<Link>} for polymorphic rendering
- Mock user footer with Avatar + DropdownMenu; DropdownMenuTrigger render={<SidebarMenuButton>} pattern
- AppHeader component with SidebarTrigger + Breadcrumb; pageTitles lookup from pathname
- Root layout wraps: ThemeProvider > TooltipProvider > SidebarProvider > AppSidebar + SidebarInset
- base-ui uses `render` prop, NOT `asChild` — SidebarMenuButton, Button, DropdownMenuTrigger all use render={<Component />}
- IconChevronsUpDown doesn't exist in @tabler/icons-react — use IconSelector instead
- Removed standalone h1 from /scrobbles page (breadcrumbs handle page title now)
- pnpm format touches many files via prettier — only stage files relevant to the task

## Iteration 13: Paginated /scrobbles page with page numbers (human-hum-lwf)

- Next.js 16 `searchParams` is a Promise — must `await props.searchParams` in Server Components
- `parsePageParam()` validates/floors the page number; defaults to 1 for non-numeric/negative values
- `getPageNumbers()` helper: always shows first, last, and ±1 around current page; gaps as `null` → ellipsis
- For ≤7 total pages, show all page numbers (no ellipsis needed)
- Disabled prev/next: use `pointer-events-none opacity-50` + `aria-disabled` + `tabIndex={-1}`
- `clampedPage = Math.min(page, totalPages)` prevents requesting beyond available pages
- Total count displayed as "X scrobbles" text alongside pagination; pagination hidden when only 1 page
- shadcn PaginationLink uses `href` prop on `<a>` tags — works without client JS (Server Component compatible)

## Iteration 14: Scrobble detail page with basic stats (human-hum-g29)

- `getScrobbleById()` fetches scrobble row then parallel COUNT queries for track/artist play counts
- Separate count queries (not `getStats`) for simplicity — only need two numbers, not the full stats object
- `ScrobbleDetail` type extends `ScrobbleRow` with `trackId`, `artistId`, `albumId`, and play counts
- Next.js 16 `params` is also a Promise (like `searchParams`) — must `await props.params`
- `notFound()` from `next/navigation` returns the built-in 404 page — no custom error handling needed
- Detail page uses `<dl>` (description list) for metadata grid — semantic HTML for key-value pairs
- Linked track names in scrobbles list to detail page with `<Link href={/scrobbles/${row.id}}>` — no 'use client' needed

## Iteration 15: Extract SourceFetcher interface (human-hum-207)

- Split `importScrobbles` into source-agnostic orchestration (`source-fetcher.ts`) and source-specific fetching (`LastfmFetcher` class)
- `SourceFetcher` interface: `source` property, `fetchPage()` returns `{ listens, totalPages, skippedCount }`, optional `getRemotePlaycount()`
- Orchestration handles: incremental sync detection, backfill resume, pagination loop, progress callbacks, completeness check
- `LastfmFetcher` class encapsulates API key/user, builds URLs, parses LastFM JSON into `ListenInput[]`
- Skipped/now-playing counting split: `skippedCount` from fetcher (no timestamp), dedup skips counted in orchestrator
- Re-exported `importScrobbles` from `lastfm.ts` for convenience — CLI wrapper just adds `new LastfmFetcher(apiKey, user)` as second arg
- Added `./importers/source-fetcher` to package.json exports map for direct access to interface/types
- No test changes needed — the refactor preserved all behavior, just reorganized code boundaries

## Iteration 16: Auto-sync on page visit (human-hum-749)

- Created `syncProbe()` function: lightweight check using fetchPage with pageSize=1 to get newTrackCount from totalPages
- Added `NowPlayingTrack` type to `FetchPageResult` — source-agnostic, extracted by each fetcher
- `LastfmFetcher.fetchPage` now extracts now-playing track info from `@attr.nowplaying === "true"` entries
- `syncProbe` sets `from = latest - 1s` (same overlap strategy as incremental sync) or undefined for empty DB
- `needsSync` is true when `totalPages > 0 && listens.length > 0` — pages with only now-playing (no completed listens) don't trigger sync
- Server action `silentSync()` in `apps/web/app/actions/sync.ts`: probes, then conditionally runs `importScrobbles` (incremental mode)
- `SyncTrigger` client component: fires once on mount via `useEffect` + `useRef` guard, uses `startTransition` for non-blocking execution
- `router.refresh()` called only when new data was actually imported — avoids unnecessary re-renders
- Re-exported `syncProbe`, `SyncProbeResult`, `NowPlayingTrack` from `lastfm.ts` for convenience imports
- 5 new tests: no new data, new tracks available, multiple pages, now-playing extraction, empty DB handling

## Iteration 17: Now Playing banner (human-hum-5er)

- Extended `SyncTrigger` to store `nowPlaying` state from `silentSync()` result — no extra API calls
- `NowPlayingBanner` rendered inline in `SyncTrigger` — avoids prop drilling or context for a simple case
- Moved `SyncTrigger` from after `<main>` to between `<AppHeader>` and `<main>` so banner appears below header
- Used `bg-primary/10 text-primary border-primary/20` for theming — adapts to light/dark mode automatically
- `animate-pulse` on `IconMusic` for visual "now playing" indicator
- `truncate` on text container prevents layout overflow with long track/artist names
- Imported `SyncResult` type from server action — Next.js allows type imports from `"use server"` files
- No new dependencies or shadcn components needed — just Tailwind + Tabler icon

## Iteration 18: Syncing indicator for catch-up sync (human-hum-n5p)

- Split `silentSync()` into `probeSync()` + `runSync()` server actions — probe returns fast, sync runs separately
- Client calls probe first, shows `SyncingBanner` if `newTrackCount > 1`, then calls runSync
- `SyncingBanner` uses existing `Spinner` component from `@workspace/ui` — no new dependencies
- Banner dismisses when sync completes; `router.refresh()` only fires if `imported > 0`
- `SyncResult` type simplified to just `{ imported: number }` since probe result is now separate
- New `ProbeResult` type includes `newTrackCount` from `SyncProbeResult` for client-side decision making
- Both banners (syncing + now playing) can coexist — syncing shows above now playing via fragment ordering
